const { AUTH_RESPONSE_DATA, WITHE_LISTED_IP } = require("./auth.data");
const { RESPONSE_DATA } = require("./response.data");

const auth = {
    AUTH_RESPONSE_DATA,
    WITHE_LISTED_IP,
};
const apiData = {
    RESPONSE_DATA,
};

module.exports = { auth, apiData };